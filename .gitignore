.venv/
__pycache__/
traces/raw/*.tmp
*.tmp
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
models/
.obsidian/workspace*.json
/data
transient_work/
.DS_Store

.venv/
__pycache__/
*.stale-*.json
# run logs are diagnostics, not results; the traces, launch records and resource records are the record
traces/*.log
traces/*.out
traces/rechecked_*
# run trajectories and stored solutions are regenerated by re-running the jobs (README, Reproducibility)
traces/raw/
traces/raw_contaminated/
traces/sol/
