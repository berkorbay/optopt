# Research journal — Track B, 2026-09-23 (morning session, Berk: "time until 09:30")

- **07:44** Started #17 (B0 go/no-go). Box: both model servers up (attended, serving stays on — Track B is CPU only),
  24 GB available → 10 CPU lanes, launch gate 8 GB.
- **07:47** Verified SCIP 10 accepts mid-solve changes from an event handler: on tr12-30 a switch at 6 s to DFS
  priority + aggressive heuristics raised mean node depth 25.9 → 34.7 and changed `heuristics/rins/freq` (25 → 8).
- **07:52** B0 design (time forced it small): 45 non-trivial MIPLIB instances (bands SHORT 9 / MEDIUM 12 / HARD 6 /
  unsolved-in-300 s 18, seed 17), 30 s, seeds 0 and 1, 12 arms:
  static D (default), DFS, BFS, HEU (aggressive heuristics), NOC (separation off);
  schedules DFS→D and DFS→BFS at the first incumbent, HEU→D and NOC→D at 25 %, D→BFS / D→DFS / D→HEU at 50 %.
  Gate: oracle over schedules vs best static, scored on the held-out seed; spec threshold 15–20 %.
- **08:02** Interim B0 (13 of 45 instances, both seeds): best static = **NOC (separation off)**, 13.9 % better than
  SCIP default on P(30) and 14.7 % on PDI(30). Held-out-seed oracles over all 12 arms add only **+2.6 % (P) /
  +1.6 % (PDI)** over NOC; same-seed oracle would show ~5 % (noise). No schedule beats NOC as a universal choice.
  Interim reading: gate (15–20 %) failing — the gain inside SCIP at 30 s is from one static change (cuts off), not
  from switching mid-solve. Wait for the full 45.
- **08:15** **Heterogeneous cores are a noise source in every run so far.** GB10 = 10 × Cortex-X925 (3.9 GHz, cores
  5–9, 15–19) + 10 × Cortex-A725 (2.8 GHz, cores 0–4, 10–14). SCIP on tr12-30, 20 s, pinned: X925 4,686 nodes /
  180k LP iterations vs A725 2,413 / 89k — **~2× the search per second**. Track A and B0 lanes were unpinned, so each
  run got a random 1×/2× effective budget. Unbiased between arms (random placement) but it inflates run-to-run
  noise and part of the "oracle headroom"; the held-out-seed scoring absorbs some of it. Fix from now on: timed
  runs pinned one-per-core to X925 (`00_baseline.py --pin 5,6,7,8,9,15,16,17,18,19`); A725 for untimed work
  (features, analysis, node-limited rollouts); GPU for Laya. → limitation in the paper for Track A and B0.
- **08:21** **Incident, my fault.** the host's memory watchdog HARD floor (MemAvailable 5.1 GiB) → shed the operator's remote
  sessions (Berk lost control) and `the Ornith-1.5-35B-A3B server (vLLM)`. Cause: 10 concurrent SCIP runs (each allowed 6 GB) next to both
  model servers; the runner's 8 GB gate only checked at launch. the remote sessions came back via
  `an automatic session restore.timer`; another (Berk's) session put `the Ornith-1.5-35B-A3B server (vLLM)` on hold until the B0 driver exits and wrote
  a restore script to restart it. Berk's rule (08:3x): the operator's sessions, databases,
  running cron jobs, the chat relay, critical systems first; the two local agents (the fast chat agent (Hermes Agent with Ornith-1.5-35B-A3B, NVFP4 on vLLM); the deep-research agent (Hermes Agent with Qwen3.8-27B, UD-IQ3_XXS on llama.cpp)) may be stopped temporarily. → D-003.
  Checked after: databases/chat relay/gateway/scheduler all up; two failed units of an unrelated project date from 09-20/09-21
  (pre-existing, unrelated). The pinned confirmation run was dropped so as not to compete with ornith's restart.
- **08:36** **B0 final (45 instances × 12 arms × 2 seeds, 1080 runs, 0 errors) — GATE FAILS.**
  Best static = NOC (separation off): P(30) 0.391 vs default 0.457 (**−17 %**), PDI(30) 0.549 vs 0.587 (−7 %).
  Held-out-seed oracle over all 12 arms vs NOC: **+0.8 % (P), +4.7 % (PDI)** — far below 15–20 %. Same-seed oracle
  would claim +6 % / +11.5 % (noise). No schedule beats NOC as a universal choice (next: NOC→D@25 0.399). Per-instance
  winners: NOC 15, D 11, NOC→D@25 8, rest ≤ 3. **Reading: at 30 s the gain inside SCIP is one static change (cuts
  off), not switching node selection / heuristics / cuts mid-solve.** Caveats: 12 hand-picked schedules, 30 s only
  (cuts pay later — longer budgets may differ), unpinned cores.
  Racing replay: ordered by Track A data, the same verdict with 65 % of the runs (random order 92 %, circular 46 %).
- **08:40** Recovery complete: the Ornith-1.5-35B-A3B server (vLLM) restarted by Berk's session's restore script at 08:36:45, healthy 08:40, test generation OK, hold released; the agent status check all three active; remote sessions + the operator's desktop session active; gateway running; no the host's memory watchdog event after 08:21. Runner now enforces per-run MemoryMax (3 GB), live 20 GB gate, lane budget check. No further runs this session.
- **08:47** Berk: larger budgets and realistic memory are fine; idle agents may be stopped within the time budget;
  document allocated resources for the paper. Both agents idle → `the Ornith-1.5-35B-A3B server (vLLM)` + `the Qwen3.8-27B server (llama.cpp)` stopped with
  the host's service-hold list until 09:30 and an **independent** restore timer (a restore timer, 09:20, script
  a restore script). No local-model scheduled job due before 12:01.
  **B1** launched: SCIP 120 s, 4 arms (default, separation off, off→default at 25 %, off→default at 50 %), the 30 B0
  instances with the largest spread among those arms, seed 0; **10 lanes pinned one per X925 core, 8 GiB hard cap
  per run (verified live), SCIP limits/memory 7 GiB**, 112 GB available at start. Hypothesis: cuts off early for
  fast primal, cuts back on for the bound — does a mid-solve switch win once the budget is long enough for cuts
  to pay? Resources now recorded per run set (`RESOURCES-*.json`) and in [[Compute resources]] / paper Table
  `tab:resources`.
- **09:05** B1 done (120/120, 0 errors, no memory warnings; min MemAvailable stayed > 25 GB). Agents restored early
  at 09:05–09:09 (both healthy, holds removed; the 09:20 timer is idempotent).
  **B1 result (SCIP 120 s, 30 instances, pinned X925, 8 GiB cap):** separation off vs default on P: **+27 % @30 s
  (Wilcoxon p=0.002), +24 % @60 s (p=0.02), +14 % @120 s (p=0.08)**; on PDI: +12 % → +7 % → **0 % @120 s** — the bound
  catches up once cuts have time to pay. **Switching back to default at 25 % or 50 % gains nothing over staying off
  (±1 % on P, −5…+1 % on PDI, all p > 0.2).** Reading: "cuts off" is a short-budget primal setting whose benefit decays
  with the budget; a mid-solve switch of *this* action has nothing to exploit up to 120 s. Selection caveat: the 30
  instances were those where these arms differed most at 30 s in B0, which favours finding a difference.
  Track B implication: the action space must change (restarts, branching rule, heuristic scheduling vs an online
  bandit) or the budget must reach the regime where default wins the bound (>120 s).
- **10:19–10:24** Day window (Berk: "calm day"): both agents idle → stopped; the host's service-hold list until 17:05; local-model scheduled
  jobs (2 health checks 12:01, an unrelated host job 13:40) paused and listed in the host's job-hold list so the host's restore timer (12:00)
  does not resume them; independent restore timer `optopt-agents-restore-day` at 17:00 (restarts both, releases holds,
  resumes the three jobs). 112 GB available.
- **10:24** **B2 sequential gate** launched: 12 SCIP arms (static D, NOC, PSC pscost branching, INF inference
  branching, HOFF heuristics off, HEU aggressive; dynamic restart@25 %, restart after 20 % stall, PSC→D@25, D→PSC@25,
  HEU→HOFF@25 "burst", HOFF→HEU after 20 % stall), 2 seeds, 120 s, blocks of 5 instances ordered by Track A
  discriminativeness; stop when the bootstrap CI of held-out headroom clears or misses 15 %. **Racing arms would be
  the wrong tool here** (an arm bad on average can be a per-instance winner) — instances are sequential instead.
  Pinned X925, 9 GiB cap, resources auto-recorded. Mid-solve restart (`restartSolve` from the event handler) and
  branching-priority changes verified to take effect.
- **10:40** #25: SCIP runs now store the final incumbent; `analysis/check_solutions.py` re-reads the model with HiGHS
  and checks bounds/integrality/rows/objective in exact rational arithmetic (tol 1e-6). Positive and negative
  (corrupted solution) tests pass.
- **10:50** #26: property tests (Hypothesis) found a **real edge in the metric**: an incumbent better than the reference
  was scored as gap 1 (Berthold's definition assumes the reference is optimal). In our 3,768 traces: 21 runs
  below the reference by ≤ 7e-6 (tolerance) and **one impossible value: cuOpt C1 on dano3_3 reported objective 0.0 vs
  proven optimum 576.34** — a placeholder objective on an error status (code 5); its only real incumbent (890.08)
  came after the budget. New rule: better than ref by ≤ 1e-4 relative → gap 0; by more → invalid, gap 1. cuOpt
  wrapper now takes the final objective only from solution-carrying statuses. Rebuilt tables: **unchanged at 3
  decimals** (portfolio, B1). 16 tests pass.
- **10:50–11:00** B2 driver crashed after block 0 on my own RESOURCES json in traces/raw (loader read it as a trace); ~9 min idle. Resource records now go to `traces/resources/<set>/` (moved the two existing ones); driver restarted and resumed without re-running block 0. Block 0 (5 inst): headroom P +2.6 % [−5.1, +9.3], PDI +6.4 % [−19.6, +17.4].
- **11:50–12:10** Berk: "settings depend on the run?" — yes. B2 at 20 instances: **PDI winner is mostly run-level**
  (oracle that sees the run +24.5 % over best static vs held-out instance oracle +4.0 %; same winner on both seeds for
  only 35 % of instances; per-arm seed noise 0.081 vs across-arm spread 0.110). P is mostly instance-level (+23.8 % vs
  +18.0 %, 70 % agreement). Static/dynamic split (held-out): P static selection +16.1 %, dynamic increment **+1.9 %
  [−1.1, +6.9]**; PDI +1.7 % / **+2.3 % [−3.0, +9.2]**. The gate as coded measures the total — it could say GO on
  static selection alone; the verdict will be scored on the dynamic increment.
- **12:10** `analysis/switch_sim.py` (offline, exact from B2 traces): at tau continue, or restart into another static
  setting (base events ≤ tau + the other run shifted by tau; incumbent and bound carried). 24 instances / 48 runs,
  120 s. Hindsight oracle vs continue: **+18 % (PDI, base D, tau 10 s), +5 % (20 s), +3 % (30 s)**; base NOC +14 % (PDI)
  / +12 % (P). **LightGBM on the run's own state at tau: −1.5 … −16 % (worse than continuing)** — it restarts too
  often. Reading: run-level headroom exists but is reachable only early (restart discards work) and is not predicted
  by these simple state features on 48 runs. Conservative simulation (a real restart keeps presolve/learned info).
  Next data: all 45 instances, more seeds per (instance, setting) — the learner needs many runs, not instances.
- **12:15** Parallel portfolios in SCIP (offline, exact, 25 instances, 120 s): same setting × 2 seeds in parallel +8.6 % (P, NOC) / +8.7 % (PDI, PSC) over the best single setting; best pair of settings +13.3 % (P, NOC‖INF) / +10.9 % (PDI, D‖NOC). No prediction needed — beats every learned controller so far. Uses 2 cores: the resource-fair baseline is SCIP's own 2-thread concurrent mode (to add in the extra-seed phase).
- **12:40** Berk: main experiment = agent in the loop, end to end (D-004); Laya both as agent and as a tool (2×2 with
  LLM); "machine is yours". LLM arms wait for Berk's choice of models / hosted-on-public-data (conservative default).
- **12:45** Agent API (#28): agents are called from SCIP's event handler every `interval` s; SCIP's limit is wall clock
  (timing/clocktype=2, verified with a 3 s sleep inside the callback — charged to the run). Agents: online bandit
  (UCB1 over 6 static settings, reward = relative pd-gap reduction + 0.5 per new incumbent, 10 s interval) and a
  rules agent from today's results (NOC at start, one restart at 10 s if no incumbent, INF when the bound stalls).
  Decision cost ≈ 0.01–0.03 ms. Chain: B2 → extra seeds (6 static × seeds 2–4 × top-20, 360 runs) → pilot E
  (default / bandit / rules, 120 s, 2 seeds, 20 ML4CO test + 15 MIPLIB not used for the rules = 210 runs).
  Parallel-seeds arm for the pilot is exact from the default runs (2 cores, reported as such).
- **13:31** **B2 final (45 instances × 12 arms × 2 seeds, 120 s, pinned, capped).** The coded gate (total) ended
  INCONCLUSIVE (P +13.6 % [+1.5, +23.4], PDI +8.9 % [−2.0, +16.1]). **Decomposed (the right quantity):**
  static selection per instance P +10.8 % [−0.7, +19.3], PDI +7.1 % [−3.7, +12.9]; **dynamic increment on top
  P +2.8 % [−0.4, +7.9], PDI +1.7 % [−1.2, +5.5]**; run-level part (hindsight, not predictable up front) P 5.5 pts,
  PDI 12.3 pts; same winner on both seeds P 62 %, PDI 38 %. Best static: NOC (P), PSC (PDI).
  **Verdict: NO-GO for mid-run switching of SCIP settings (restart, branching rule, heuristic scheduling, cuts) at
  120 s** — upper bounds ≤ 8 %. What remains: per-instance static choice (~7–11 %) and run-level variance (exploited by
  parallel copies, +9–13 %, no prediction). Extra seeds started 13:31.
- **14:25** Extra seeds done (360 runs, 0 errors). 5 seeds × 6 static settings × top-20 instances, 120 s: best static NOC (P 0.310, PDI 0.475); instance-level static oracle (seed-averaged choice) **P +7.7 %, PDI +14.0 %**; per-seed winner = seed-averaged winner in 75 % (P) / 64 % (PDI) of runs (the 2-seed 38 % was two noisy runs compared with each other); seed s.d. 0.032/0.064 vs between-setting s.d. 0.095/0.103. Parallel copies of NOC, k = 2/3/5: P +7.1/+9.4/+11.5 %, PDI +4.1/+5.6/+7.8 % (diminishing). → The agent's main target is picking the right static setting early (esp. for the bound). Pilot started 14:23.
- **15:03** **Main-experiment pilot done** (D-004; 35 held-out instances = 20 ML4CO test + 15 MIPLIB not used for the
  rules, × 2 seeds, 120 s, 1 X925 core + 9 GiB, agents start from default, every decision charged to the wall clock;
  183/183 solutions verified; agent decision time < 0.1 ms). vs default on P(120): **online bandit +8.2 % (p = 0.003,
  38W/24L) — ML4CO +20.4 % (p = 0.0005), MIPLIB +1.6 % n.s.**; rules agent −2.6 % n.s.; 2× default on 2 cores +10.9 %
  (61W/0L; ML4CO +23.8 %). PDI: no agent gains (bandit −0.5 %, 2× default +5.4 %). At 30 s the bandit is still exploring
  (+0.2 %). **First end-to-end positive: an untrained online bandit on one core gets most of what a second core buys on
  the generated families.** It is the baseline Laya must beat. Rules from MIPLIB averages do not transfer.
- **15:08** Confirmation run `pilot_e2`: 30 fresh ML4CO test instances (15 + 15, indices 10–24), default / bandit (10 s) /
  bandit (5 s), 2 seeds, 120 s.
- **15:42** **Confirmation `pilot_e2`** (30 fresh ML4CO test instances × 2 seeds, 120 s, 1 core; 180/180 solutions
  verified): P(120) vs default — **bandit (10 s) +21.3 % (p = 2e-5), bandit (5 s) +27.8 % (p = 2e-4)**; item placement
  +24.5 % (29W/1L) / **+32.2 % (28W/2L)**; load balancing −0.7 % / −2.8 % (p = 0.045). At 60 s the 5 s bandit is already
  +12.9 % (p = 0.002). PDI small (+1–2 % on item placement, significant). The bandit spends ~1/3 of item-placement time in
  HEU (aggressive heuristics) — consistent with Track A (SCIP S1 beat S0 by ~22 % on item placement at 30 s): it may be
  mostly *finding the right static setting online*. Test launched: static HEU and static NOC from t=0 on the same 60
  (instance, seed) pairs.
- **16:09** **Static control on the same 60 (instance, seed) pairs:** fixed HEU (aggressive heuristics) from t=0 is at
  least as good as the bandit — item placement P(120) HEU 0.240 vs bandit(5 s) 0.247 (n.s.), at 60 s HEU better by
  12.9 % (p = 0.047); load balancing NOC best (0.044) vs bandit 0.050. **The bandit's gain is finding the right static
  setting online, paid for by exploration; it never beats the best static setting for the family.** Consistent with
  B2: the value is in *choosing* the setting, not in *switching* during the run. The agent's job is to identify the
  right setting as early as possible — ideally before starting (Track A's question, now per family with a clear target,
  where a trained model reading the instance could beat an online bandit).
- **16:13** Window closed early: both agents restored (test generation OK), all optopt hold entries removed, three
  local-model jobs resumed; **an unrelated host job missed today's 13:40 run** (paused over its slot) and is
  re-anchored to 16:13 daily — left for Berk. Health checks next 22:13. the remote sessions active, gateway running.
- **16:30–17:40** Paper restructured on Berk's request: lean body (claim; setup; headroom; prediction before the solve;
  choosing vs switching; agent in the loop; parallel copies; discussion) with compact tables; all technical detail and
  the experiments without a positive result (B0, B1, restart simulation, rule agent, racing) moved to the appendix.
  Title: "Choose Early, Don't Switch". Fixed a wrong caption claim (not all non-oracle intervals include zero).
- **17:45** Solution checker was too slow on large instances (exact Fraction over every row) and I had started it on
  all 20 cores — stopped. Now: float pass over all rows + exact re-check of any row that could be near the tolerance;
  runs on A725 cores only. 483/483 pilot solutions pass; B2 check running.
- **17:57** Evening window (agents idle; stopped, hold until 20:40, restore at chain end or timer 20:30).
  **Diagnosis experiment** (the paper's next step): 6 SCIP settings × 60 ML4CO *training* instances (120 s) → LightGBM
  cost regressors on the 62 static features predict the best setting per instance at t = 0; scored exactly on the 30
  held-out pilot_e2 test instances (missing settings PSC/INF/HOFF filled in there) against default, best fixed, family
  majority, bandit (10 s / 5 s), static oracle; then a diagnose-then-bandit agent (predicted setting first, bandit
  fallback) is run on the test set.
- **19:10–20:02** SCIP recorder lag found (tutorial example) and corrected exactly; everything re-scored (conclusions
  unchanged; "separation off" advantage smaller; Laya fine-tuned selector now just excludes zero at 10 s, trained on
  pre-correction labels — retrain pending). Tutorials (6 chapters, figures, examples) and open-access audit + reading list
  done; citation policy recorded. **E3 diagnosis:** best fixed setting from training +28.5 %, LightGBM per-instance +23.4 %,
  diagnose-then-bandit +29.1 % (best agent, ties the fixed setting). Agents restored 20:02 by hand after the chain's
  restore step killed itself (fixed).
- **Prior art (evening):** four sweeps, 159 works, following Berk's 18-step legal workflow → `40-literature/prior-art/`.
  Verdict: every function we give Laya exists singly in one solver (DASH switching, Hendel phases, bandits, BenLOC,
  Lawless/GRIMIP LLM configuration, ReXi racing); not found: learned cross-solver selection incl. a GPU solver, a small
  fine-tuned encoder, the static-vs-switching decomposition — negative results, so no "first" claim. Paper: related work
  rewritten, C3 scoped (DASH/Balans), parallel copies framed as a measurement, appendix `app:related` with the paywalled
  disclaimer (8 works). Also fixed a wrong caption on the selection table (said no interval excludes zero; Laya ft 10 s
  does; pooled n is 163, not 313).
