# Research journal — 2026-09-23 (night session, AgentRA; box until 07:00)

- **00:20** Brief received. Box state: both local model servers up (~80 GB), the deep-research agent (Hermes Agent with Qwen3.8-27B, UD-IQ3_XXS on llama.cpp) mid-run, 23 GB free.
  Berk chose: stop both local model servers for the window; approved all downloads.
- **00:25** Repo `berkorbay/optopt`. uv venv: highspy 1.15.1, pyscipopt 6.2.1 (SCIP 10.0),
  cuopt-cu13 26.8.0, torch 2.14 cu130. All wheels, no compiles.
- **00:30** Downloads: MIPLIB 2017 benchmark (333 MB, 240 instances), PGLib-UC (git), ML4CO instances.tar.gz
  (5.6 GB, Google Drive link from ds4dm/ml4co-competition START.md), Laya checkpoint (843 MB).
  The first Laya download also pulled the multilingual/typed-decisions sub-models (a CLI glob mistake);
  stopped after 36 MB, those two folders are unused.
- **00:35** Smoke test on 50v-10 (opt 3311.18), 10 s each: H0 3473.1 / H1 3473.1 / S0 3859.3 / S1 3477.0 /
  C0 6382.3 / C1 4618.3. cuOpt overruns its time limit by 2–3 s (GPU setup + teardown); metrics are cut at the
  budget so the overrun earns nothing. H0 and H1 identical here — heuristic effort did not matter within 10 s.
- **00:40** MIPLIB subset: 226 eligible (≤2.5M nnz, not infeasible, easy/hard/open) → one per MIPLIB group
  (194) → 80 stratified by status × size tercile, seed 20260923. The benchmark set is ~90 % "easy" by
  MIPLIB's definition (solved within 1 h by *some* solver), so the subset is too; "easy" ≠ easy in 60 s.
- **00:45** PGLib-UC: converted with the benchmark's *own* `uc_model.py` (executed up to its solve call) to MPS,
  so the formulation is exactly the published one. ~20 s per case.
- **00:46** Metrics: Berthold primal integral / primal-dual integral / time-to-first / time-to-target (1 %), cut
  at budget; 6 unit tests pass.
- **00:50** Think's 00:00 run completed on its own (checked the scheduler's run log: completed). Window opened:
  `the Ornith-1.5-35B-A3B server (vLLM)` + `the Qwen3.8-27B server (llama.cpp)` added to `the host's service-hold list` until 07:00 and stopped (both still `enabled`);
  paused the host's model health checks and an unrelated research job. The the host's log channel notice was blocked by a
  permission prompt in this session — nobody was told on the chat relay; this journal is the record.
  After stop: 110 GB available, GPU empty.
- **00:50** MIPLIB run started: 12 CPU lanes (HiGHS/SCIP 1 thread each) + 2 cuOpt lanes (3 CPU threads each).
  Measured: cuOpt run = 62–63 s for a 60 s budget; CPU half done in ~22 min, GPU half ~85 min.
- **00:53** Laya smoke test: 421.3 M parameters (ModernBERT-large + 2-layer head), prompt 263 tokens,
  single decision 55.7 ms median while two cuOpt lanes were running (so those two cuOpt runs saw ~1 min of
  extra GPU load — noted, not rerun). Load 41 s cold. Peak GPU memory 4.8 GB.
- **00:55** Run chains (systemd user units `optopt-chain-{cpu,gpu}`) queue ML4CO → UC → variance per lane,
  waiting on log counts / DONE markers. Variance split: CPU strategies seeds 1–4, cuOpt seeds 1–2 (GPU time).
  All Laya GPU work (fine-tune, contention) is scheduled AFTER the solver runs so it cannot bias cuOpt timings.
- **00:57** PGLib-UC: 56 instances, ~250k rows × ~250k cols each (log10 5.4). A stray duplicate converter
  process raced the unit on gzip; all 56 files re-read cleanly with HiGHS. Budget raised to 60 s for UC
  (30 s would likely leave every strategy without an incumbent → no signal).
- **00:58** Literature subagent: 68 verified BibTeX entries (`paper/refs.bib`), [[Related work]], [[Laya]].
  Key point from the model card: base Laya is near chance zero-shot (0.362 vs 0.461 majority) and its
  6–10-option temperature is uncalibrated (1.00) — zero-shot is an infrastructure check, not a contender.
- **01:08** HiGHS H0 and H1 on MIPLIB `s100` ignored `time_limit=60` and were killed by the runner at 150 s
  (budget×1.5+60). Scored as failure (cost 1.0, no incumbent). This is conservative: any incumbent found before
  60 s is lost with the killed process. Likely a long uninterruptible phase (presolve/root LP) — HiGHS checks
  the time limit only between phases. Kept as a real property of the strategy under a hard wall-clock budget.
- **01:57** cuOpt C1 on MIPLIB `sorrell3` did not return and was killed at 150 s (budget×1.5+60); scored as failure. C0 on the same instance: ok 65.7s.
- **02:08** MIPLIB (80 × 6, seed 0) complete. Mean P(T) and oracle headroom over the hindsight SBS:
  | T | C0 | C1 | H0 | H1 | S0 | S1 | SBS | VBS | headroom |
  |---|---|---|---|---|---|---|---|---|---|
  | 10 | .523 | .548 | .532 | .528 | .626 | .621 | C0 | .364 | 30 % |
  | 30 | .407 | .440 | .413 | .406 | .498 | .497 | H1 | .249 | 38 % |
  | 60 | .341 | .380 | .348 | .337 | .415 | .418 | H1 | .203 | 40 % |
  Wins (ties split) at 60 s: H1 24, C0 17, H0 16, S1 9, C1 8, S0 5 of 80. Reached 1 % gap within 60 s: best
  single strategy 44/80 (H1), any strategy 56/80. **Phase-1 go/no-go (>10 %) passes on MIPLIB** — with the
  caveat that a single-seed oracle profits from run-to-run noise; the variance subset will size that.
  The SBS flips with the budget (cuOpt best at 10 s, HiGHS-heuristic at 30/60 s): the budget itself is a
  decision variable, which supports spec §14's dynamic allocation.
- **02:08** Early UC (CPU, 36 of 56): SCIP S0/S1 find no incumbent in 60 s on any case (P=1.000);
  HiGHS 0.97 / 0.91. cuOpt pending.
- **02:10–02:21** My own analysis run (01_selection preview) used all cores (LightGBM default threads + torch) while 12 solver lanes and 2 cuOpt lanes ran — ~10 min of CPU contention on runs in flight in that window (UC CPU, variance CPU, ML4CO cuOpt). Stopped; LightGBM/torch now pinned to 1 thread. Affected runs are identifiable by log timestamp; re-run them at the end if CPU time allows (issue: rerun-contaminated-window).
- **02:20** Selection preview, MIPLIB only (5-fold, ~64 train instances per fold, 62 features):
  closed gap vs hindsight SBS at 10/30/60 s — LightGBM +0.06/−0.09/−0.10, MLP +0.13/−0.23/−0.23, rules
  −0.21/−0.11/−0.17, best-fixed(train) −0.29/−0.29/−0.29; all CIs cross 0. Against the *realistic* baseline
  (best fixed chosen on training folds) LightGBM cuts mean P(T) 0.568→0.514 / 0.451→0.420 / 0.375→0.351 and
  time-to-1 % sgm 14.2→12.2 s / 29.1→23.2 s / 37.4→30.1 s. Reading: on 80 heterogeneous instances the
  headroom is large (30–40 %) but mostly not *predictable* from static features with this little data.
  The hand rules (pre-registered) do about as well as LightGBM at 30/60 s.
- **02:52** GPU lanes behind plan: ML4CO cuOpt 154/500 at 02:50 (3.7 runs/min) → UC and cuOpt-variance would
  end ~06:00, leaving no GPU window for Laya/contention. Cut (D-001 addendum): ML4CO training set 75 per family
  (was 100; test 25 per family unchanged), cuOpt variance seeds dropped (CPU strategies keep seeds 1–4).
  Stopped `optopt-chain-gpu` (2 in-flight cuOpt runs lost, re-queued), restarted as `optopt-chain-gpu2` with
  `experiments/jobs/ml4co_gpu.jsonl` then UC. Expected GPU done ≈ 04:55.
- **03:00** Variance subset (12 MIPLIB, HiGHS/SCIP × seeds 0–4, P(60)): median per-instance s.d. 0.004–0.012,
  mean s.d. 0.02–0.04, but single instances swing up to 0.63 (S1). Per-seed winner = seed-averaged winner on 68 %
  of (instance, seed) pairs. **Noise inflation of the oracle is small:** a single-seed oracle looks 36.8 % better
  than the SBS on its own seed, its choices realise 31.3 % on the held-out seeds, the true (seed-mean) oracle is
  34.6 %. So ≈85 % of measured headroom is real; the go/no-go conclusion survives noise.
- **03:05** CPU smoke test of the Laya fine-tune path (2 threads, 4 examples, ~1 min) while miplib300 CPU runs were going.
- **03:30** 300 s screening (H0, S0 on the 80 MIPLIB, 12 concurrent) → spec bands by the faster default's time to
  optimality: trivial 10, SHORT 11, MEDIUM 16, HARD 6, unsolved-in-300 s 37. **Complementarity by difficulty**:
  H1 (HiGHS heuristic-heavy) is the best fixed strategy in every solvable band (P(60) .077/.166/.200/.249), but on
  the 37 instances neither CPU default solves in 300 s, cuOpt C0 is best (.426 vs H1 .532, S0 .601). Band is not
  known at t=0 — it is a hindsight label — but it motivates the spec's phase-5 rule "start on HiGHS, switch to
  cuOpt when the search looks hard", which static features cannot express.
- **03:28** Re-running the 172 CPU runs (68 UC, 104 variance) that overlapped the 02:10–02:21 contention; originals
  kept in `traces/raw_contaminated/` to measure the effect. The 43 ML4CO cuOpt runs in that window cannot be
  re-run tonight (GPU busy) → limitation.
- **03:39** Contamination check: 104 variance runs re-run clean — mean P(60) 0.1966 contaminated vs 0.1953 clean, mean |diff| 0.016 (same order as seed noise). UC re-runs: no incumbents in either (P undefined vs best-run ref). Conclusion: the 02:10–02:21 window had negligible effect; the 43 cuOpt runs left as-is are flagged, not excluded.
- **03:45** **Offline CPU‖GPU pairs** (`analysis/pairs.py`, exact from recorded trajectories — they were all
  recorded under the same 12+2-lane concurrency): H1‖C0 (HiGHS heuristic-heavy on 1 CPU thread + cuOpt on the GPU,
  best incumbent of the two) is the best pair in every family: MIPLIB P(60) 0.337 (SBS) → **0.220** (oracle 0.203,
  ≈88 % of headroom); ML4CO item placement 0.213 → 0.202 (oracle 0.182); load balancing 0.092 → 0.066 (oracle over
  single strategies 0.067); pooled 0.212 → 0.162 (oracle 0.150, ≈81 %). Sequential switches (restart, no solution
  transfer) are much weaker (pooled 0.213). **This is the headline candidate: on a DGX Spark, the cheapest way to
  capture the portfolio headroom is not to choose but to run the CPU and GPU solver side by side.** Caveat: the pair
  uses more resources than one strategy; it is the concurrency the box offers for free only if nothing else wants
  the GPU. (ML4CO counts partial: cuOpt still running.)
- **04:00** ML4CO cuOpt done (75+25 per family; CPU strategies 100+25). Selection, MIPLIB + ML4CO, seed 0:
  *Pooled* (n=130 test = 80 MIPLIB + 50 ML4CO valid; best-fixed(train) = H1 = hindsight SBS):
  T=10 LightGBM closed gap **+0.32 [0.01, 0.55]**, MLP +0.21 [−0.05, 0.41], rules −0.22; T=30 LightGBM −0.03
  [−0.47, 0.28], MLP −0.11, rules −0.26. *LOFO* (n=280): best-fixed H1 wins at 30 s (learned −0.21 … −0.52);
  at 10 s MLP −0.06, LightGBM −0.17 vs best-fixed −1.32 (hindsight SBS on the pooled test set is not H1 at 10 s).
  Reading: learned selection pays at the *short* budget (where cuOpt's fast start competes with HiGHS) and when
  the family is seen; it does not transfer to an unseen family, and at 30 s "always H1" is as good.
  Pair H1‖C0 with full ML4CO: pooled 0.203 → 0.155 (oracle 0.144), ≈81 % of headroom.
- **05:01** All solver runs done (GPU chain 05:01). UC: 23 of 56 cases got no incumbent from ANY strategy in 60 s
  (no reference → excluded from P); on the 33 others cuOpt C0 is the SBS (P 0.640) and H1‖C0 0.568 ≈ oracle 0.574.
  Final non-Laya selection, all four families (pooled n=163 test, LOFO n=313):
  pooled T=10 LightGBM **+0.17 [−0.16, 0.43]**, MLP −0.02, rules −0.21; T=30 LightGBM −0.13, MLP −0.05, rules −0.23.
  LOFO T=30: best-fixed(train) (H1) beats every learned policy (LightGBM −0.58, MLP −0.68, rules −0.51).
  Pair H1‖C0: pooled 0.254 → 0.199 vs oracle 0.190 (≈86 % of headroom), best pair in every family.
  Laya T=30 fine-tuning started 05:01.
- **05:14** Laya T=30 done (fine-tune 69–98 s per fold, 8 epochs, ~11 s/epoch on GB10; peak GPU mem 15.4 GB in
  training). Single-decision latency on an idle GPU **31.6 ms median / 34.6 ms p90**.
  T=30 closed gap — pooled (n=163): MIP-Laya **−0.06 [−0.28, 0.13]**, MLP −0.05, LightGBM −0.13, rules −0.23,
  Laya zero-shot −0.97 [−1.84, −0.38]. LOFO (n=313): MIP-Laya **−0.38** (best learned), LightGBM −0.58, MLP −0.68,
  rules −0.51, zero-shot −1.79; best-fixed (H1) 0. MIP-Laya picks H1 for 113/163 — it mostly learned the SBS,
  which is also why it degrades least on unseen families. Zero-shot picks C0/S1 almost constantly (as the model
  card predicts for an un-fine-tuned task). **Verdict at 30 s: no learned selector beats "always H1"; MIP-Laya
  ties the MLP and edges LightGBM — per the spec's rule, no case for Laya over the simpler models on this task.**
- **05:28** Laya T=10 done. Closed gap at 10 s — pooled: **MIP-Laya +0.19 [−0.02, 0.37]**, LightGBM +0.17
  [−0.16, 0.43], MLP −0.02, rules −0.21, zero-shot −0.73. LOFO: **MIP-Laya +0.04 [−0.05, 0.13]** — the only learned
  policy ≥ SBS out of family (LightGBM −0.10, MLP −0.27, rules −0.41, best-fixed(train) −1.22). So at the short
  budget the fine-tuned model is the best selector by a hair, and the most robust across families; at 30 s none
  beats H1. One fine-tuning seed only → not a claim, a lead. The contention experiment (bug fixed) started 05:28.
- **05:40** After the clean re-runs the variance numbers moved slightly: winner agreement 65 % (was 68), apparent 37.1 % / realised 31.8 % / true 34.8 % (was 36.8/31.3/34.6). Paper and results note use the new ones.
- **06:01** Contention (spec §15) done: 5 MIPLIB × {A,B,C,D} × 2 reps, cuOpt C0 30 s, Latin-square order.
  Mean P(30): A 0.226, B (Laya loaded idle) 0.240 (+0.014, median paired diff −0.004), C (decision every 5 s)
  0.235 (+0.009, median 0.000), **D (continuous inference) 0.452 (+0.226, worse in 10/10)**. SM clock 2.39–2.41 GHz
  throughout (no throttling) → the contention is for GPU/memory bandwidth, not power. Laya latency beside cuOpt:
  30 ms median / 50 ms p90 (C), 26/44 (D). Rule: sequential or low duty cycle only.
- **06:02** Window closed early: removed my two `the host's service-hold list` lines, started `the Ornith-1.5-35B-A3B server (vLLM)` + `the Qwen3.8-27B server (llama.cpp)`
  (both active + enabled), resumed the host's model health checks (next 12:01 — re-anchored) and
  an unrelated research job (next 2026-09-24 00:00).
- **06:10** Paper draft final for tonight: 13 pages, all tables generated from `datasets/`, 0 undefined refs,
  0 overfull boxes. Checkpoint note `vault/90-checkpoints/2026-09-23-0700.md` written. Session closed; nothing
  left running (`optopt-*` units finished, serving restored and verified with a test generation on both ports).
- **(morning)** Berk: the primary question is in-solver dynamic control of one solver (depth-first / best-first etc. on the fly). Night-1 work kept as Track A (tag `track-a-night1`), stays in the paper. Track B plan written; no compute started. D-002.
