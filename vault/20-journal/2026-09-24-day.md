# Journal — 2026-09-24 (day)

Berk: "do both, start with llm agent arms" (LLM-agent arms, then the exchange through a SCIP heuristic plugin); approved
stopping the deep-research agent (Hermes Agent with Qwen3.8-27B) for the grid; add the hosted model as an arm; "go gpt 6
for all" (the author's hosted-agent setup now uses GPT-6 Sol; the experiment uses GPT-6 Luna because of the Plus quota);
documentation names harness + model + spec, not house pseudonyms.

## Incident 07:06
Uncapped Laya fine-tune on the GPU beside both local models → the host's memory watchdog HARD floor → the operator's remote sessions shed
(restored 07:10), Ornith server stopped (restarted ~07:30). No cron run lost. Lesson: cgroup MemoryMax does not see
GPU allocations here (peak 508 KiB reported for a 1.6 GB load) — GPU jobs need a MemAvailable check and must not run
beside both models. [[Mistakes and incidents]].

## LLM-agent arms (D-004 2×2), 07:36–10:23
30 held-out ML4CO test instances × 2 seeds (GPT 1 seed), 120 s, 1 X925 core + 8 GiB, 4 lanes; 330 runs, 0 errors.

| arm | P(120) | vs default | vs best fixed | thinking |
|---|---|---|---|---|
| SCIP defaults | 0.184 | — | −46 % | — |
| best fixed (HEU, from training) | 0.126 | +31.7 % | — | — |
| Laya agent (fine-tuned, t = 0) | 0.116 | +37.0 % | +7.9 % (p 0.10) | 0.1 % |
| Ornith-1.5-35B-A3B (local, every 20 s) | 0.115 | +37.5 % | +8.5 % (p 0.018) | 1.3 % |
| Ornith + Laya as a tool | 0.121 | +34.6 % | +4.3 % (n.s.) | 1.5 % |
| GPT-6 Luna, reasoning low (every 30 s) | 0.137 | +25.3 % | −15.3 % (n.s.) | 10.1 % |

- The untrained local LLM makes the family-level first move (aggressive heuristics) about as well as fine-tuned Laya.
- The ~8 % edge of both over the fixed setting is the timing of the same choice (applied after the start vs before
  presolve) plus noise — Laya picks HEU on item placement exactly like the fixed arm.
- Laya's probabilities as a tool did not help the LLM; GPT-6 Luna lost on latency (median 3.3 s, p95 9.2 s per call).
- PDI: all arms within 3 %.
- Latency: Laya 75 ms, Ornith 0.33 s (p95 0.67), GPT-6 Luna 3.3 s. Quota used by the GPT arm: ~2 % session, ~1 % weekly.

## Exchange via heuristic plugin
`_XchgHeur` (SCIP heuristic, BEFORENODE | DURINGLPLOOP) replaces event-handler injection. First test on h80x6320d:
cuOpt's solutions accepted at 2.9 s and 3.3 s during the root phase (final 12,526 vs 16,952 without). Full rerun of
race / g2c / both on 80 MIPLIB (prefix `xheur`) in progress.

## Afternoon: two external reviews, fixes, and the control experiments
- **Review 1** (repository review, `transient_work/`): PDI integration bug confirmed and fixed (exact-time merge, regression
  tests); the claim "switching helps the bound at 300 s" was an artifact and is withdrawn; gate unified with the
  decomposition; references fail loudly and have a manifest; `OPTOPT_DATA` round-trips; run identity (config hash);
  strict trace JSON; agent state at BESTSOLFOUND; instance-level tests everywhere (LLM vs fixed becomes n.s.).
  Response and status: [[2026-09-24 repository review — response and plan]].
- **Review 2** (second review): session drift between days (0.7–2.7 %, one direction) → within-session rule in the paper;
  resource record regression fixed; library copy put under git; stale generated texts removed; HiGHS and cuOpt now
  store their final solutions for the checker.
- **Timing-matched controls** (`timing_ctrl`, 300 runs, one session): the fixed setting applied at the first callback beats
  the same setting before presolve by 10.9 % (24/30, p 0.005); against it the LLM agent is −10.6 % (p 0.004); later LLM
  decisions add +3.6 % (n.s.); the earlier +8.5 % over the fixed setting does not replicate (+1.5 %).
- **Live HiGHS‖cuOpt** (`xhighs`, interleaved with HiGHS alone): +30.4 % over HiGHS alone (61/13).
- **Solution re-check with the fixed checker:** all sets clean except b2_scip's 4 known tolerance-boundary rows
  (1.0000001e-6, neos-3046615-murg, aggressive heuristics) and the deliberate corrupt fixture in `_smoke` (detected);
  llm_agents and diag_train checked for the first time (660, 0 failed).
- Running into the evening: same-session SCIP‖cuOpt vs SCIP alone (`xsame`), final re-checks, final `optopt reproduce`.

## Late afternoon — same-session race, checker coverage, lean repository
- SCIP‖cuOpt re-run with race and SCIP-alone interleaved in one session: +21.5 % (44 of 80 instances, 28/13, p 4e-5); the run
  stopped when the experiment window closed at 16:04. The earlier +26 % compared against SCIP alone from another session, which
  ran 3.9 % slower on the same instances (38 of 44 slower) — session drift. Paper, limitations and AGENTS.md now quote 21.5 %.
- Solution checker coverage: 4,332 stored solutions (4,125 SCIP, 67 HiGHS, 140 cuOpt), 4,326 within 1e-6. Failures: the known
  four SCIP solutions at 1e-13 relative, and two cuOpt solutions at up to 3e-6 integrality (cuOpt's default tolerance is 1e-4).
- A timing-control run improved the best-known value of item_placement_9913; reference manifest regenerated.
- Run logs and the historical orchestration scripts removed from the repository and ignored (Berk: "keep reproducibility
  lean"); host scheduler and service names redacted from the vault.
