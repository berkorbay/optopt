# Checkpoint — 2026-09-23 07:00 (end of first night)

## Where things stand
- **Milestone 1 answered** → [[Milestone 1 — static strategy selection]]. Headroom real (pooled 21–23 %, MIPLIB
  30–40 %, ~85 % survives seed noise). Static learned selection captures little: at 10 s fine-tuned Laya +0.19 and
  LightGBM +0.17 closed gap (CIs touch 0); at 30 s nothing beats "always H1". Zero-shot Laya unusable.
  **H1‖C0 pair captures ~86 % of headroom with no model** (offline, exact from traces).
- Paper draft `paper/main.pdf` compiles (12 pages, 68 verified refs). **Not submitted.** Author line and
  the AI-agent acknowledgement need Berk's review.
- Issues closed: #1 infra, #3 phase 0, #4 phase 1, #5 phase 2, #7 phase 4. Open: #2 (BenLOC), #6/#9–#13 follow-ups,
  #8 contention, #14 paper, #15 this checkpoint, #16 pair.

## Contention (spec §15)
cuOpt C0 30 s, 5 MIPLIB × 2 reps: alone 0.226; Laya loaded-idle +0.014 and one decision per 5 s +0.009 (both
noise); **continuous Laya inference doubles cuOpt's P (0.452, 10/10 worse)** at unchanged SM clock → bandwidth
contention. Laya 30 ms beside cuOpt. Rule: sequential / low duty cycle. Issue #8 closed.

## What was cut and why (D-001 + 02:52 addendum)
Budgets 30/60 s (not 300); ML4CO 75–100 train + 25 test per family (not 1000/100); cuOpt variance seeds; BenLOC;
10-seed runs; dynamic control, LNS, SCIP internals, UC scenarios. GPU (cuOpt) was the bottleneck all night.

## Known data caveats
- 3 runs ignored the wall limit and were killed (HiGHS H0/H1 on `s100`, cuOpt C1 on `sorrell3`) → P = 1.
- 02:10–02:21 my analysis job loaded the CPU; 172 CPU runs re-run (effect negligible), 43 ML4CO cuOpt runs kept.
- UC: 23/56 cases no incumbent from anyone in 60 s; UC refs are best-found, not proven.
- Laya: one fine-tuning seed per fold; hyper-parameters untuned.

## Next steps (proposed, in order)
1. **Run the pair live** (#16): HiGHS H1 thread + cuOpt concurrently, then cooperative incumbent exchange
   (cuOpt `SetSolutionCallback`, HiGHS `setSolution`), vs the offline number.
2. **Dynamic, state-based decisions** (#9): features from the first 2–5 s of both solvers (incumbent count, gap,
   LP stats) → "which pair / keep or reallocate". This is where a text-encoder policy could differ from LightGBM.
3. Longer budgets (120–300 s), 3+ seeds including cuOpt, full ML4CO training sets; Laya with 3 fine-tune seeds.
4. BenLOC (#2), PGLib-UC scenario generation (#13).

## Box state at hand-off
Window closed **06:02** (early). `the Ornith-1.5-35B-A3B server (vLLM)` + `the Qwen3.8-27B server (llama.cpp)` active **and enabled**, both answered a test
generation at 06:05; my `the host's service-hold list` lines removed (the older expired `the Ornith-1.5-35B-A3B server (vLLM) until 2026-09-22T20:45` line
was left as found). Resumed: the host's model health checks (next 12:01, re-anchored by the pause),
an unrelated research job (next 2026-09-24 00:00). the scheduler status: gateway running. the agent status check: all three
bridges active and idle. No the host's log channel notice was posted when the window opened (permission prompt denied in
this session) — this note and the journal are the record.

## How to resume
`cd ~/git_repositories/optopt && source .venv/bin/activate`. Re-derive every table:
`python analysis/build_runs.py && python analysis/pairs.py && python experiments/01_selection.py --budgets 10,30
--laya datasets/laya_preds.json && python analysis/reports.py` (single-threaded; ~2 min). Raw traces
`traces/raw/` are committed (22 MB); data `data/`. Run grids with `experiments/00_baseline.py <jobs>`.
**Never run analysis with default thread counts while solver lanes are running.**

## Addendum (morning, after Berk's review)
Berk: the primary question is **in-solver dynamic control of a single solver**, not cross-solver selection. The
night's work is kept as **Track A** (tag `track-a-night1`) and will stay in the paper. The "Next steps" above
are superseded by [[Track B — in-solver dynamic control]] (B0 headroom of fixed schedules first). The CPU‖GPU
pair (#16) remains a Track A follow-up.

## Addendum 08:45 — Track B B0 and an incident
- **B0 gate failed** (45 MIPLIB × 12 SCIP settings/schedules × 2 seeds, 30 s): best static "separation off" is 17 %
  better than SCIP default; mid-solve schedules add +0.8 % (P) / +4.7 % (PDI) on held-out seeds. #17.
- **Incident 08:21** — my B0 grid caused a the host's memory watchdog shed (the operator's remote sessions, the Ornith-1.5-35B-A3B server (vLLM)).
  Recovered by 08:40; rules in [[D-003 Memory budget and priorities beside serving]]; runner now hard-caps each run.
- Next Track B step, if Berk wants it: longer budgets (120–300 s), restart/branching actions, pinned X925 lanes,
  racing instead of full grids — within D-003's memory rules.

## Addendum 09:15 — B1 and session close
- **B1** (SCIP 120 s, 30 instances, pinned X925, 8 GiB cap, agents stopped 08:47–09:05): separation-off advantage decays
  with budget (P: 27 → 24 → 14 %; PDI: 12 → 7 → 0 %); switching cuts back on mid-solve gains nothing. Track B needs a
  different action space (restart, branching, heuristic scheduling vs online bandit), not a different model.
- Resources for every experiment: [[Compute resources]] + paper Table `tab:resources`; the runner now writes
  `RESOURCES-*.json` per run set.
- Verification issues opened: #25 (solution vectors + exact feasibility checker — do first), #26 (property tests),
  #27 (optional Lean-verified checker).
- Box at close: both agents active + enabled and healthy (09:09), holds removed, no optopt unit running,
  a restore timer fires once at 09:20 and is a no-op.

## Addendum 16:20 — day session (Track B round 2 + main-experiment pilot)
- **B2** (45 MIPLIB × 12 SCIP arms × 2 seeds, 120 s, pinned, capped): mid-solve switching (restart, branching,
  heuristic scheduling, cuts) adds **+2.8 % (P) / +1.7 % (PDI)** over the per-instance static choice (upper bounds 8 / 5.5 %)
  → **NO-GO for switching**. Best setting depends on the run too (5 seeds: per-seed winner = mean winner 75 % P / 64 % PDI);
  parallel copies of the best setting: k = 2/3/5 → +7/+9/+12 % (P).
- **Main experiment (D-004) pilot:** untrained online bandit agent, one core, beats SCIP default end to end — +8 % overall,
  **+21–28 % on fresh ML4CO** (item placement +32 %) — but a fixed "aggressive heuristics" setting from t=0 is at least as
  good: the gain is finding the right static setting online.
- **Where this leaves the project:** the agent's value is *early diagnosis* (pick the right setting at or near t=0).
  Next: (1) instance→setting prediction per family (LightGBM vs Laya) as the agent's first move, bandit as fallback;
  (2) LLM arms of the 2×2 once Berk picks models; (3) longer budgets (300 s+) where cuts and bound work matter;
  (4) verification: solution vectors for Track A runs (#25).
- New this session: agent API (#28), bandit + rules agents, exact solution checker (#25, 0 failures on 453 solutions),
  property tests (#26, found and fixed a metric edge), resource records per run set, core pinning, per-run memory caps.
- Box at close (16:13): both agents active + enabled + generating; holds released; optopt units none running.
  an unrelated host job missed today's run (re-anchored to 16:13 daily) — Berk's call.
